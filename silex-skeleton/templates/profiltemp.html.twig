
{% extends 'layout.html.twig' %}
{% set active = 'accueil' %}

{% block content %}
    <!-- 
    <div>
        <div class="container">
            <div class="row">
                <div class="span3 offset1">
                  <!-- <img src="{{ app.request.basepath }}/img/users/{{ membres.id_personne }}.jpg" class=""></img>
                </div>
                <div>
                  <h1 id="user_name">{{ user.username|e }}</h1>
                  <dl class="dl-horizontal">
                      <dt>{{ 'email:'|e}}</dt>
                      <dd>{{ membres.mail|e }}</dd>
                      <dt>{{ 'member for:'|e }}</dt>
                      <dd>{{ memberDepuis }}</dd>
                  </dl>
                </div>
            </div>
        </div>
    </div> -->
    -->
{% endblock %}
